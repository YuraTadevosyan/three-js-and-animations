{
  "name": "interactive-museum",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "A portfolio you walk through instead of scroll. Six rooms — Entrance, Experience, Projects, Career, Skills, Contact — each with its own lighting rig, procedurally synthesized spatial ambience, kinetic animation and a hidden interaction to discover. First-person controls with a guided tour fallback. Babylon.js 9 + SolidJS.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b --force",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist -e interactive-museum"
  },
  "dependencies": {
    "@babylonjs/core": "^9.19.0",
    "solid-js": "^1.9.14"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "autoprefixer": "^10.5.4",
    "gh-pages": "^6.3.0",
    "postcss": "^8.5.23",
    "tailwindcss": "^3.4.19",
    "typescript": "^6.0.3",
    "vite": "^8.1.5",
    "vite-plugin-solid": "^2.11.14"
  }
}
